<!DOCTYPE html>
<html>
<head>
<style>
table {
  font-family: arial, sans-serif;
  border-collapse: collapse;
  width: 100%; 
}

td, th {
  border: 1px solid #dddddd;
  text-align: left;
  padding: 8px;
}

tr:nth-child(even) {
  background-color: #dddddd;
}
</style>
</head>
<body>

<h2>Table</h2>
<%= link_to "Recive Email", emails_path, class: "btn btn-info", remote: true, method: :post%>
  <div class="container d-flex flex-row">
    <div class="col-6 border">
      <h2 class="border">Liste des emails</h2>
        <div class="card-title">
          <ul id="email-list">
          <% @email.each do |email|  %>
            <p><%= link_to "#{email.object}", email_path(email), remote: true %>
            <br><%= email.body %><p>
          <% end %>
          </ul>
        </div>
    </div>
      <div class="col-6 border">
        <h2 class="border">Emails sélectionnés</h2>
        <div id=target></div>
      </div>
      <div class="w-100"></div>
  </div>